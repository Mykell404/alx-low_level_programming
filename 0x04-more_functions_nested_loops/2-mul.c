#include "main.h"
/**
 * mul - a multiply function
 * @a: first integr
 * @b: second integer
 * Return: result the result of the function
 */

int mul(int a, int b)
{
	int result = a * b;

	return (result);
}
